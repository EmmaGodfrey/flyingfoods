export function SettingsPage() {
  return (
    <section className="card" style={{ marginTop: 0 }}>
      <h3 className="card-title">Settings</h3>
      <p className="card-subtitle">Settings shell route is ready for future preferences, role controls, and profile modules.</p>
    </section>
  );
}
