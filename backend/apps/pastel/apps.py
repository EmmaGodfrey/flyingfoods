"""App configuration for the Pastel integration placeholder."""

from django.apps import AppConfig


class PastelConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.pastel"
    verbose_name = "Pastel Integration"
